# DFT+DMFT Workbench 当前状态

> 核对日期：2026-07-21  
> 当前代码已恢复到 2026-07-17 的原始工作台基线；本文件只记录现役状态和仍需处理的问题。

## 权威入口

- 使用和启动方式：`README.md`
- Agent 修改边界与数据安全：`AGENTS.md`
- 系统结构：`doc/架构图.md`
- 数据库字段：`doc/数据字典.md`
- HTTP 接口：`doc/接口清单.md`
- 实际行为最终以当前代码、SQLite schema 和本地运行态为准。

## 当前功能

- Project 表示一个材料或研究体系；Task 表示该项目的一次完整工作流运行；Step 是工作流模板中的步骤。
- One-shot DFT+DMFT 模板包含 5 个阶段、20 个步骤，其中 2 个步骤可选。
- 支持项目/任务 CRUD、步骤进度、笔记、自定义命令、LSF 脚本和文件关联。
- 支持项目工作目录浏览、文本读取和目录创建；创建任务时按阶段建立 `{任务名}/{stageId}/` 目录。
- 工作流模板保存在 `workflow_templates` 表中，可通过 UI 编辑或重置为源码内置默认值。
- HPC 页面保存项目级配置并生成 LSF/scp 命令供用户复制；不建立 SSH 连接，也不自动上传、提交或下载。
- 支持经验库及项目、任务、步骤软关联。
- **研究方案模块**（2026-07-21 新增）：保存与 AI 讨论得出的研究规划，包含研究目标、理论背景、方法学、执行步骤、预期结果、讨论与决策记录六大字段，支持关联项目和任务、状态管理（草稿/进行中/已完成/已归档）、版本递增、全文搜索。研究方案可在侧边栏"研究方案"入口访问，也可在项目详情页的"研究方案"Tab 中按项目查看。

## 2026-07-21 验证结果

- `npm ci`：依赖按锁文件重建成功。
- `npm run lint`：通过，保留 9 条现有 warning。
- `npm run build`：通过，生成单文件 `dist/index.html`。
- 本地页面 `http://localhost:3001` 返回 HTTP 200；项目和工作流只读接口可用。
- 运行数据库为 6 张业务表，不包含后来试验的 Task Spec、作业链或 Agent 同步表。

## 待处理问题

1. **文件路径边界需要修复**：`server/routes/files.ts` 当前使用字符串前缀判断，无法可靠区分同名前缀目录。修复前不要把服务暴露给不受信任的请求方。
2. **缺少自动化回归测试入口**：`package.json` 没有 `test` 脚本；历史上的手工 E2E 结果不能替代当前回归测试。
3. **后端独立类型检查失败**：根 `tsconfig.json` 不引用 `server/tsconfig.json`，所以 `npm run build` 不覆盖后端；单独执行 `npx --no-install tsc -p server/tsconfig.json --noEmit` 会因嵌套路由参数类型及 `rootDir`/NodeNext 配置报错。现役服务通过 `tsx` 运行，不代表这些类型错误已解决。
4. **依赖审计告警**：2026-07-21 执行 `npm ci` 后报告 2 个 high severity 漏洞；尚未升级，以免在本次基线收尾中引入行为变化。
5. **HPC 仍是人工复制粘贴流程**：现役代码没有 SSH、网页终端桥接或智能体执行能力。

## 历史摘要

- 2026-07-09：从 localStorage 原型重构为 React + Express + SQLite 工作台。
- 2026-07-10：加入任务阶段目录、自定义步骤命令和文件关联联动。
- 2026-07-11：加入数据库化工作流模板、模板编辑器、项目级 HPC 配置和 LSF 复制向导。
- 2026-07-21：撤销此后试验性的稳定性/HPCPlus/Agent/复杂任务系统改造，恢复上述原始基线。
- 2026-07-21：新增"研究方案"模块（`research_plans` 表 + `/api/research-plans` 路由 + 研究方案页面 + 项目详情页 Tab 集成）。
