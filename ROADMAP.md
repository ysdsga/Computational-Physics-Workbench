# 未来路线图

> 从可用的 Codex + LSF 研究原型，走向可靠、有科学记忆、可插拔的计算与理论物理协作基础设施。

这是一份方向说明，不是发布时间承诺。优先级以真实研究中的失败代价为准：先保证任务不会失联、结论不会遗失，再扩展客户端、超算和物理方法。

## P0 · 让研究可靠地持续下去

- **不中断的长任务**：Job 结束不等于 Task 结束；自动恢复 Working Plan，完善排队过久检查、heartbeat 生命周期和异常对账，杜绝“作业关闭、研究也悄悄停止”。
- **安全隔离**：检测并处理不干净的工作目录；把研究执行环境与 Workbench 源码开发环境隔离，避免执行研究的 Agent 意外修改产品后端。
- **稳定启动与访问**：解决 `workbench` CLI 的 PATH/环境刷新问题，完善自诊断；补齐生产地址的默认 CORS 行为，消除 `Origin is not allowed by Workbench` 的常见误报。
- **并行与规模**：验证多个 Task/Run 并行时的目录、Job、monitor 和账本隔离；为大量 Event、Artifact 和 Evidence 增加分页、增量加载与性能基线。

## P0 · 建立真正的科学记忆

- **科学主张台账**：引入一等公民式 Claim，直接连接“物理主张—支持/反对证据—Artifact—Run—适用条件—当前有效性”，让重要结果不再埋在报告和时间线里。
- **经验闭环**：在规划和排错前主动检索适用经验，在验证后自动形成候选经验，并处理重复、冲突、失效与适用边界，减少同类错误反复发生。
- **可持续知识底座**：建立有来源、可更新、可审计的权威知识库与价值/判断准则库，同时严格区分外部事实、Agent 推断和研究者结论。

## P1 · 从硬编码参考路径走向可插拔生态

- **计算设备与调度器 Adapter**：保留多个设备/profile 与 Task 绑定模型，把 LSF 实现抽象为稳定接口，再用同一契约支持 Slurm、PBS 等调度器，并发布真实兼容性矩阵。
- **Agent 客户端 Adapter**：以 Codex 为参考实现，为 Claude Code、WorkBuddy、DeepSeek Harness 等补齐 Skill、Hook、长作业唤醒、权限和恢复测试；“能调用 API”不等于正式兼容。
- **卡片式工作流**：从“修改源码注册模板”升级为启动时发现 `workflows/*.json|yaml`，完成 schema 校验、版本迁移、ID 冲突处理和错误隔离，让社区贡献者放入一个文件即可使用。
- **显式 Capability**：把理论推导、符号计算、文献检索和高级物理工具包纳入 Task Spec capability，而不是依赖未声明的 Agent 环境能力。

## P2 · 扩展科学推理与物理能力

- **更强的理论研究循环**：把“检查最接近问题答案的已有成果”设为明确关口；加入可限时、可暂停、有反思记录的深度探索模式，而不是无边界地增加思考时间。
- **专业工具环境**：提供可探测、可复现、按需安装的符号代数、群论、张量网络、数值积分等工具配置，并记录版本与适用边界。
- **更多多体方法**：在真实案例验证后增加 DMFT vertex、cluster DMFT 等工作流；参数、近似和物理判断继续留在 Research Plan，不硬编码进执行器。
- **研究方案体验**：恢复元数据和小修订的清晰入口；更直观地展示跨 Run lineage、Envelope revision 及其对结论有效性的影响。

## 我们尤其欢迎的贡献

- 能稳定复现“任务中断、经验失忆、并行冲突或大数据卡顿”的最小案例；
- 新调度器或 Agent adapter 的契约设计与端到端测试；
- Claim–Evidence 数据模型、科研记忆检索和冲突处理方案；
- 经真实研究验证、材料无关的计算物理或理论物理工作流卡片。

提议新功能前请先阅读 [CONTRIBUTING.md](CONTRIBUTING.md)。小而完整、可回滚、带失败复现与验证证据的贡献会被优先考虑。
