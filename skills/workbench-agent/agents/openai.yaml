interface:
  display_name: "DFT+DMFT Workbench Agent"
  short_description: "Run researcher-approved work from the Codex conversation"
  default_prompt: "Use $workbench-agent to recover the current Workbench context, discuss the research plan/workflow and exact manifest with me, then perform only the action I explicitly authorize through the bounded CLI."
