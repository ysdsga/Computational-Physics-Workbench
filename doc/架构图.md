# DFT+DMFT Workbench 架构图

> 最后核对：2026-08-15（Codex 驱动的 Workbench V2）

## 整体分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        浏览器 (Browser)                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│ │  React SPA (HashRouter)                                    │  │
│ │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │  │
│ │  │项目/方案  │  │工作流     │  │Agent中心 │  │评价/证据  │   │  │
│ │  │ProjectsPg│  │WorkflowPg│  │AgentRuns │  │Reviews   │   │  │
│ │  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │  │
│ │       │             │            │             │          │  │
│ │  ┌────┴─────────────┴────────────┴─────────────┴────┐     │  │
│ │  │           API Client (src/api/client.ts)          │     │  │
│ │  │  fetch wrapper → 封装所有 HTTP 请求               │     │  │
│ │  └───────────────────────┬───────────────────────────┘     │  │
│ └───────────────────────────┼───────────────────────────────┘  │
└──────────────────────────────┼──────────────────────────────────┘
                               │ HTTP (fetch /api/*)
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Express Server (localhost:3001)               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    中间件层 (Middleware)                    │  │
│  │  cors → express.json → 路由分发 → static(dist/) → SPA fallback│
│  └───────────────────────────┬───────────────────────────────┘  │
│                              │                                   │
│  ┌───────────────────────────▼───────────────────────────────┐  │
│  │              Controller 层 (server/routes/*.ts)            │  │
│  │  接收 HTTP 请求，解析参数，调用 Service 逻辑，返回 JSON     │  │
│  │                                                            │  │
│  │  ┌─────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │  │
│  │  │projects.ts  │ │tasks.ts  │ │progress  │ │files.ts   │  │  │
│  │  │             │ │          │ │  .ts     │ │           │  │  │
│  │  └──────┬──────┘ └────┬─────┘ └────┬─────┘ └─────┬─────┘  │  │
│  │  ┌──────────┐  ┌──────────┐                            │  │  │
│  │  │agent/contracts│  │remote   │                          │  │  │
│  │  └────┬──────────┘  └────┬────┘                          │  │  │
│  └───────┼─────────────┼──────────────────────────────────┘  │
│          │             │                                        │
│  ┌───────▼─────────────▼──────────────────────────────────┐    │
│  │     Service + DAO 层（Agent 内核已拆到 services）          │    │
│  │  agentCore/pathSafety/taskRoots/remote + routes SQL       │    │
│  │  ┌─────────────────────────────────────────────────┐    │    │
│  │  │  db.ts (better-sqlite3 实例)                     │    │    │
│  │  │  → Schema 初始化                                 │    │    │
│  │  │  → WAL 模式 + 外键约束                           │    │    │
│  │  └──────────────────────┬──────────────────────────┘    │    │
│  └─────────────────────────┼──────────────────────────────┘    │
│                             │                                    │
│  ┌──────────────────────────▼──────────────────────────────┐    │
│  │              数据存储层 (Data Layer)                      │    │
│  │  ┌──────────────┐  ┌────────────────────────────────┐  │    │
│  │  │ SQLite       │  │ 文件系统 (项目 working_dir)       │  │    │
│  │  │ 19 张业务表   │  │ → Task 根、方案、合同与科研文件     │  │    │
│  │  └──────────────┘  └────────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

## 前端组件架构

```
src/
├── main.tsx                      # 入口，挂载 React
├── App.tsx                       # 路由配置 (HashRouter)
├── index.css                     # Tailwind CSS v4
│
├── api/
│   └── client.ts                 # API 调用封装层
│       ├── projectsApi           #   .list() .get() .create() .update() .delete()
│       ├── tasksApi              #   .list() .get() .create() .update() .updateWorkflow() .delete()
│       ├── progressApi           #   .list() .upsert()
│       ├── stepFilesApi          #   .list() .add() .remove()
│       ├── filesApi              #   .browse() .mkdir() .read()
│       ├── experiencesApi        #   .list() .create() .update() .delete()
│       └── workflowsApi          #   .list() .listFull() .getFull() .save() .reset()
│
├── data/
│   └── workflows.ts              # 内置模板默认值（首次播种与重置回退）
│
├── contexts/
│   └── WorkflowContext.tsx       # 从 API 加载/保存现役模板并向组件共享
│
├── types/
│   └── index.ts                  # 全部 TypeScript 类型定义
│
├── pages/                        # 页面组件
│   ├── ProjectsPage.tsx          # 项目仓库列表
│   ├── ProjectDetailPage.tsx     # 项目详情（任务列表 + 文件仓库 Tab）
│   ├── TaskWorkflowPage.tsx      # 任务独立工作流（编辑 + 进度追踪）
│   ├── WorkflowPage.tsx          # 工作流入口（模板预览 + 任务选择模式）
│   ├── AgentRunsPage.tsx         # Agent 运行记录与命令预览
│   ├── ReviewCenterPage.tsx      # 待沟通事项筛选与决定记录
│   ├── SupercomputersPage.tsx    # 连接元数据、边界、传输与作业观察
│   ├── EvidencePage.tsx          # 项目/任务证据索引
│   └── ExperiencePage.tsx        # 可复用经验记忆
│
└── components/                   # 可复用组件
    ├── Sidebar.tsx              # 工作台侧边栏导航
    ├── Flowchart.tsx            # SVG 交互式流程图
    ├── StepDetail.tsx           # 步骤详情面板（状态/笔记/文件关联）
    ├── ProgressBar.tsx          # 进度条（总体 + 各阶段）
    ├── FileBrowser.tsx          # 文件浏览器（目录导航/文件查看）
    └── TemplateEditor.tsx       # 工作流阶段/步骤可视化编辑器
```

## 后端路由挂载关系

```
Express App (index.ts)
│
├── /api/workflows                    → workflows.ts
│   ├── GET    /                       模板元数据列表
│   ├── GET    /all/full               所有完整模板
│   ├── GET    /:id                    单个完整模板
│   ├── PUT    /:id                    保存模板
│   └── POST   /:id/reset              重置为内置默认
│
├── /api/projects                     → projects.ts
│   ├── GET    /                       列出所有项目
│   ├── POST   /                       创建项目
│   ├── GET    /:id                    获取单个项目（含 task_count）
│   ├── PUT    /:id                    更新项目
│   └── DELETE /:id                    删除项目（级联）
│
├── /api/projects/:projectId/tasks    → tasks.ts (mergeParams)
│   ├── GET    /                       列出项目下任务
│   └── POST   /                       创建任务
│
├── /api/tasks                        → tasks.ts
│   ├── GET    /:taskId                获取任务（含进度）
│   ├── PUT    /:taskId/workflow       保存当前任务的独立工作流
│   ├── PUT    /:taskId                更新任务
│   └── DELETE /:taskId                删除任务（级联）
│
├── /api/tasks/:taskId/progress       → progress.ts (mergeParams)
│   ├── GET    /                       获取所有步骤进度
│   ├── PUT    /:stepId                更新步骤状态/笔记
│   ├── GET    /:stepId/files          列出步骤关联文件
│   └── POST   /:stepId/files          添加文件关联
│
├── /api/step-files/:fileId            → index.ts 内联
│   └── DELETE                          删除文件关联
│
├── /api/projects/:projectId/files     → files.ts (mergeParams)
│   ├── GET    /?path=                  浏览项目工作目录
│   ├── POST   /mkdir                   创建子目录
│   └── GET    /read?path=              读取文件内容
│
├── /api/experiences                  → experiences.ts
│   ├── GET    /                       列出经验（支持搜索/过滤）
│   ├── POST   /                       创建经验
│   ├── GET    /:id                    获取单条
│   ├── PUT    /:id                    更新
│   └── DELETE /:id                    删除
│
├── /api/research-plans/:id/contract → contracts.ts
│   ├── GET                             读取合同和 drift
│   ├── POST /initialize                创建最小合同
│   └── PUT                             校验并保存合同
│
├── /api/agent/v1                    → agent.ts
│   ├── /context/tasks/:taskId         统一 AgentContextV1
│   ├── /runs + /events                运行、版本和追加账本
│   ├── /execution-contract             通用 capability/spec 边界
│   ├── /executable-actions             immutable manifest 提案
│   ├── /actions/:id/execute            已授权 capability 执行
│   ├── /policies                      分层授权策略
│   └── /reviews                       评价请求与唯一决定
│
├── /api/agent/v1/remote             → remote.ts
│   └── /jobs/:jobId/status|logs|reconcile  已记录 LSF 作业观察与恢复
│
├── express.static(dist/)             # 静态前端文件
└── SPA fallback middleware           # 非 API 路由返回 index.html
```

## 数据流向（典型场景）

### 场景：用户在流程图上点击步骤 → 标记为已完成

```
用户点击步骤节点
  │
  ▼
Flowchart.tsx  → onSelectStep(step) 回调
  │
  ▼
WorkflowPage / TaskWorkflowPage  → 渲染 StepDetail 组件
  │
  ▼ 用户点击"已完成"按钮
  │
StepDetail.tsx  → progressApi.upsert(taskId, stepId, { status: 'completed' })
  │
  ▼
api/client.ts  → fetch PUT /api/tasks/:taskId/progress/:stepId
  │
  ▼ HTTP
  │
server/routes/progress.ts  → db.prepare('INSERT OR UPDATE step_progress ...')
  │
  ▼
SQLite (workbench.db)  → step_progress 表更新
  │
  ▼ 返回更新后的 StepProgress JSON
  │
StepDetail.tsx  → onProgressChanged() 回调
  │
  ▼
WorkflowPage / TaskWorkflowPage  → load() 重新拉取进度
  │
  ▼
Flowchart.tsx  → 重新渲染，步骤节点变绿色
```

## 当前架构特点

### 现状
- 原有 CRUD 路由仍直接调用 SQL；Codex context/action/evidence/conclusion、通用 capability executor 与远程状态机已拆到 `server/services/`
- schema v4 有 19 张业务表；`PRAGMA user_version` 管理 V0→V4 增量迁移并生成迁移前备份
- Codex 项目对话是 Agent，`workbench` CLI 是唯一机器入口；Agent/评价/证据 Web 页面只读展示已记录状态
- 超算管理只维护连接元数据并显示有效边界、传输和作业快照；真实远程变更只从已授权 capability manifest 进入，默认环境开关关闭

### 未来可演进方向
如果项目复杂度增长，可拆分为三层：

```
routes/*.ts        (Controller)   → 只负责 HTTP 解析和响应
services/*.ts      (Service)      → 业务逻辑、数据校验、跨表事务
repositories/*.ts  (DAO)          → 纯 SQL 操作，每张表一个 Repository
```
